<?php

// Load Match Height plugin
$this->footScript()->appendFile($this->assetModule('js/jquery.matchHeight.js'));

$script = <<<'EOT'
    $(function() {
        $('.index-media .thumbnail').matchHeight();
        $('.index-media .thumbnail .caption').matchHeight();
    });
EOT;

$this->footScript()->appendScript($script);